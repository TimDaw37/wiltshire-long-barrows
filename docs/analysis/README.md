# Orientation analysis + map

Article: [wiltshire-long-barrow-orientations.md](wiltshire-long-barrow-orientations.md)

Map (serve the repo root): `index.html` — 128 barrows, 86 display axes.

```
python generate.py
python scripts/orientation_analysis.py
python scripts/orientation_robust.py
```

Read-only on NHLE `azimuth_deg`. Display axes live in `data/long_barrows.json`.
